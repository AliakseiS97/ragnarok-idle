package com.ragnarok.idle.domain;

/**
 * Рунический ряд (колонка {@code futhark}, хранится строкой).
 * Значения — DISTINCT из сида мифологии (V10): Старший и Младший Футарк.
 */
public enum Futhark {
    ELDER,
    YOUNGER
}
